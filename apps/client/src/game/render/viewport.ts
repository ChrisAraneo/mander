export interface Viewport {
  /** World units visible across the canvas. */
  width: number;
  /** World units visible down the canvas. */
  height: number;
  /** Device pixels per world unit. */
  scale: number;
}
