import type { Structure } from '@mander/structures';

export const pickStructures = (
  seed: string,
  difficulty: 'normal' | 'hard',
): Structure => {
  // TODO: Implement picking randomly 7 structures based on the seed and difficulty from the library structures lib
};
